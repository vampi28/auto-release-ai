{
  "name": "auto-release-copilot",
  "version": "1.0.0",
  "description": "GitHub Action para generar releases automáticas usando GitHub Copilot",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt",
    "package": "npm run build && git add dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-username/auto-release.git"
  },
  "keywords": [
    "github-actions",
    "releases",
    "automation",
    "copilot",
    "ai"
  ],
  "author": "Your Name",
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
